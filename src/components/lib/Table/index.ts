/**
 * Index file for Table component
 * Exports the component and its TypeScript interfaces
 */

// Import CSS variables first to ensure they're available for component styling
import './gcds-colors.css';

export { Table } from './Table';
export { TableColorTest } from './TableColorTest';
export type { TableProps, TableHeader, TableCell } from './Table.types'; 